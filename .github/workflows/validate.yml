name: Validar Script
on: [push]
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Instalar y ejecutar ShellCheck
        run: |
          sudo apt-get install shellcheck
          shellcheck script.sh

